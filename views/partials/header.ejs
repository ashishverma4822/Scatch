<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full justify-between px-5 py-3">
        <h3 class="text-xl">Scatch</h3>
        <% var loggedin = typeof loggedin != "undefined" ? loggedin : true %>
        <% if(loggedin) { %>
            <div class = "flex gap-5">
                <a href="/shop" class="text-gray-600 hover:text-gray-800">Shop</a>
                <a href="/cart" class="text-gray-600 hover:text-gray-800">Cart</a>
                <a href="/" class="text-gray-600 hover:text-gray-800">My Account</a>
                <a href="/users/logout" class="text-gray-600 hover:text-gray-800">Logout</a>
            </div>
        <% } %>
    </nav>